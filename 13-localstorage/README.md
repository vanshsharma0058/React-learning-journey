### Localstorage

- Whenever you want to store the object or array to localstorage
  then localstorage say key ,value pair must be in string ,then you use
  "json.stringify" method.
  This is necessary

## Some method

-.setitem("key","value")
-.getitem()
-.clear()
-.removeitem("key")

-to convert the object into string
use JSON.Stringify

-To convert the string into object
use JSON.parse
